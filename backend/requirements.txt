fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
# anthropic SDK — required even in fallback/demo mode (top-level import in extract.py)
anthropic>=0.25.0
# chromadb removed: enrich.py uses an embedded in-memory retriever, not chromadb
networkx>=3.0
aiosqlite>=0.19.0
python-multipart>=0.0.6
jinja2>=3.1.2
# pypdf is the modern package (successor to PyPDF2); install with: pip install pypdf
pypdf>=3.0.0
pytest>=7.0.0
pytest-asyncio>=0.23.0
